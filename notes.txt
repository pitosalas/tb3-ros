# Notes for developers
docker network prune -f; 
docker container prune -f
docker build -t tb3-ros:latest .
docker-compose up

http://0.0.0.0:6080/nonvc.html
xterm -fa default -fs 13

sudo apt update
sudo apt upgrade
sudo apt install ros-melodic-slam-gmapping

It seems like when I change the dockerfile to have more stuff in the initial my_ros_data it doesn't take when there already was stuff there.



# Errors

"WARNING: apt does not have a stable CLI interface. Use with caution in scripts."
"debconf: delaying package configuration, since apt-utils is not installed"

